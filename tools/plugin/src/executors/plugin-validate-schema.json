{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Plugin Validate executor",
  "description": "Schema for plugin validate executor",
  "type": "object",
  "properties": {
    "tsConfig": {
      "type": "string",
      "description": "Path to TypeScript configuration file",
      "default": "tsconfig.json"
    },
    "validateManifest": {
      "type": "boolean",
      "description": "Validate plugin manifest",
      "default": true
    }
  },
  "required": []
}
