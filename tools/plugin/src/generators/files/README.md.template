# <%= name %>

This plugin was generated with [modu-nest plugin generator](https://github.com/your-org/modu-nest).

## Description

<%= className %> plugin for modu-nest

## Plugin Development

### Building the Plugin

Run `nx run <%= name %>:plugin-build` to build the plugin.

### Validating the Plugin

Run `nx run <%= name %>:plugin-validate` to validate the plugin structure and manifest.

### Publishing the Plugin

Run `nx run <%= name %>:plugin-publish` to publish the plugin to the plugin registry.

### Running Unit Tests

Run `nx test <%= name %>` to execute the unit tests via [Jest](https://jestjs.io).

### Linting

Run `nx lint <%= name %>` to lint the plugin code.

## Plugin Structure

- `src/lib/<%= name %>.module.ts` - Main NestJS module
- `src/lib/<%= name %>.service.ts` - Plugin service with business logic
- `src/lib/<%= name %>.controller.ts` - REST API endpoints
- `plugin.manifest.json` - Plugin metadata and configuration